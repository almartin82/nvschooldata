# Get known enrollment file URLs

Returns a named list mapping end years to their download URLs. URLs are
from the NDE Azure storage and may change when files are updated.

## Usage

``` r
get_enrollment_urls()
```

## Value

Named list of URLs keyed by end_year
